/**
* @object	root
*
* @function jexport(name, value)
* Exports the value to the JSON result
* @param	name:string	Name of the object in the result
* @param	value:?		Value of the object to export
*
*/
/*
* @function	parseInt(string, radix)
* Parses a string argument and returns an integer of the specified radix or base.
* @param	string:string	The value to parse. If string is not a string, then it is converted to one. Leading whitespace in the string is ignored.
* @param	radix?:number	An integer that represents the radix of the above mentioned string. Always specify this parameter to eliminate reader confusion and to guarantee predictable behavior.
* @returns	number
* @link	https://developer.mozilla.org/en-US/docs/JavaScript/Reference/Global_Objects/parseInt	parseInt
*
*
* @function	parseFloat(string)
* Parses a string argument and returns a floating point number.
* @param	string:string	A string that represents the value you want to parse.
* @returns	number
* @link	https://developer.mozilla.org/en-US/docs/JavaScript/Reference/Global_Objects/parseFloat	parseFloat
*
*
* @function	isNaN(value)
* Determines whether a value is NaN or not.
* @param	value:number	The value to be tested.
* @returns	bool
* @link	https://developer.mozilla.org/en-US/docs/JavaScript/Reference/Global_Objects/isNaN	isNaN
*
*
* @function	eval(code)
* Evaluates JavaScript code represented as a string.
* @param	code:string	A string representing a JavaScript expression, statement, or sequence of statements.
* @link	https://developer.mozilla.org/en-US/docs/JavaScript/Reference/Global_Objects/eval	eval
*
*
* @function	encodeURI(uri)
* Encodes a Uniform Resource Identifier (URI) by replacing each instance of certain characters
* by one, two, three, or four escape sequences representing the UTF-8 encoding of the character (will only be four escape sequences for characters composed of two "surrogate" characters).
* @param	uri:string	A complete Uniform Resource Identifier.
* @returns	string
* @link	https://developer.mozilla.org/en-US/docs/JavaScript/Reference/Global_Objects/encodeURI	encodeURI
*
*
* @function	encodeURIComponent(str)
* Encodes a Uniform Resource Identifier (URI) component by replacing each instance of certain characters
* by one, two, three, or four escape sequences representing the UTF-8 encoding of the character (will only be four escape sequences for characters composed of two "surrogate" characters).
* @param	str:string	A component of a URI.
* @returns	string
* @link	https://developer.mozilla.org/en-US/docs/JavaScript/Reference/Global_Objects/encodeURIComponent	encodeURIComponent
*
*
* @function	decodeURI(uri)
* Decodes a Uniform Resource Identifier (URI) previously created by encodeURI or by a similar routine.
* @param	uri:string	A complete, encoded Uniform Resource Identifier.
* @returns	string
* @link	https://developer.mozilla.org/en-US/docs/JavaScript/Reference/Global_Objects/decodeURI	decodeURI
*
*
* @function	decodeURIComponent(uri)
* Decodes a Uniform Resource Identifier (URI) component previously created by encodeURIComponent or by a similar routine.
* @param	uri:string	An encoded component of a Uniform Resource Identifier.
* @returns	string
* @link	https://developer.mozilla.org/en-US/docs/JavaScript/Reference/Global_Objects/decodeURIComponent	decodeURIComponent
*/